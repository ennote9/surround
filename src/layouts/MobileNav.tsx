export function MobileNav() {
  return (
    <header className="sticky top-0 z-30 flex min-w-0 items-center border-b border-slate-200 bg-white/95 px-4 pb-2.5 backdrop-blur supports-backdrop-filter:backdrop-blur-sm lg:hidden pt-[max(0.625rem,env(safe-area-inset-top,0px))]">
      <span className="min-w-0 flex-1 truncate text-base font-semibold tracking-tight text-slate-950">
        Life Progress OS
      </span>
    </header>
  )
}
